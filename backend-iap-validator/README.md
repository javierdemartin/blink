# Blink Shell AppStore Receipt Validation

## Usage

There's a secret variable that's needed inside `main.go`. Get it on App Store Connect on Apps --> In-App Purchases --> Manage.

```bash
go run main.go
```
